# docker-compose.yml
version: '3.8'

services:
  ngx-workshop-bff:
    build: .
    container_name: ngx-workshop-bff
    restart: unless-stopped
    ports:
      - "3002:3002"
    env_file:
      - .env